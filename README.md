# Live Coding Orders App
We added the ability to sign in using jwt inside this example project. 

## Get started 
+ To get started use the following command `npm install`
+ Then use `npm run dev` to start the server. 









